import React from "react";

export default function App() {
  return (
    <div style={{padding: 24, fontFamily: "system-ui, -apple-system"}}>
      <h1>SDCH Pet Match</h1>
      <p>✅ Build sanity check: the app compiles and runs.</p>
      <p>Once this works, we’ll add the nicer animals panel back in small steps.</p>
    </div>
  );
}
